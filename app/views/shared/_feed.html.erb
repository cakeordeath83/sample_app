<% if @feed_items.any? %>
  <ol class="microposts">
    <!--  Rails knows to call the micropost partial as each element of @feed_items has the class Micropost -->
    <%= render @feed_items %>
  </ol>
<%= will_paginate @feed_items %>
<% end %>