<% provide(:title, "All users") %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <!-- Was an iteration over the @users element but refactored into this.
  When render is called with a list of objects it automatically iterates over them and renders them with the _user.html.erb partial.-->
  <%= render @users %>
</ul>

<%= will_paginate %>